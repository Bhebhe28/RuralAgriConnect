{
  "name": "ruragriconnect",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "deploy": "npm run build --prefix client && firebase deploy --only hosting",
    "server": "npm run dev --prefix server",
    "build:server": "npm run build --prefix server"
  }
}
